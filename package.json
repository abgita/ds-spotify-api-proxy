{
    "name": "ds-spotify-api-proxy",
    "version": "0.1.0",
    "description": "Dancing stickers' proxy server for a subset of the Spotify API",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "node -r dotenv/config server.js"
    },
    "dependencies": {
        "express": "^4.17.1",
        "node-fetch": "^2.6.1"
    },
    "devDependencies": {
        "dotenv": "^8.2.0"
    }
}
